[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = [
  "npm ci",
  "pip install --break-system-packages numpy pennylane scikit-learn"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run db:push && npm start"
